# Scroll-Driven-Timelines-Starting-Style-Transitions
this page uses CSS scroll-driven animations and entrance transitions without using JavaScript
animation-timeline: scroll(root), animation-timeline: view() are used to track the scroll and fade the image
prefers-reduced-motion is also used to improve performance
to view: open index.html in browser
